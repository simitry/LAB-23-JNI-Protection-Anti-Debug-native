buildscript {
    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        classpath "com.android.tools.build:gradle:9.0.1"
    }
}

plugins {
    // The Android application plugin is applied in app/build.gradle.
}
